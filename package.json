{
  "name": "drova-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ drova-technologies/drova-subgraph",
    "create-local": "graph create --node http://localhost:8020/ drova-technologies/drova-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ drova-technologies/drova-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 drova-technologies/drova-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
